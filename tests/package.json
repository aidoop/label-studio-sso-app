{
  "name": "label-studio-integration-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "Integration tests for Label Studio Custom",
  "scripts": {
    "test": "node --test integration/*.test.js",
    "test:verbose": "node --test --test-reporter=spec integration/*.test.js",
    "test:watch": "node --test --watch integration/*.test.js"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
