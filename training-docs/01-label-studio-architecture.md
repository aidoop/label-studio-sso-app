# Label Studio 아키텍처

## 목차
1. [Label Studio 소개](#1-label-studio-소개)
2. [전체 아키텍처](#2-전체-아키텍처)
3. [백엔드 구조](#3-백엔드-구조)
4. [프론트엔드 구조](#4-프론트엔드-구조)
5. [데이터 모델](#5-데이터-모델)
6. [API 구조](#6-api-구조)
7. [확장 포인트](#7-확장-포인트)

---

## 1. Label Studio 소개

### 1.1 Label Studio란?

Label Studio는 **오픈소스 데이터 라벨링 플랫폼**입니다.

**주요 특징**:
- 다양한 데이터 타입 지원 (이미지, 텍스트, 오디오, 비디오)
- 유연한 라벨링 인터페이스 구성
- ML 백엔드 연동 (예측 기반 라벨링)
- REST API 제공
- 웹 기반 UI

### 1.2 사용 사례

```
┌─────────────────────────────────────────────────────────────────┐
│                     Label Studio 사용 사례                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 이미지 분류   │  │ 객체 탐지     │  │ 텍스트 분류   │          │
│  │              │  │              │  │              │          │
│  │  🖼️ → 📋     │  │  🖼️ → ⬜     │  │  📝 → 🏷️     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 개체명 인식   │  │ 음성 전사     │  │ 비디오 분류   │          │
│  │              │  │              │  │              │          │
│  │  📝 → [NER]  │  │  🔊 → 📝     │  │  🎬 → 🏷️     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 ML 파이프라인에서의 위치

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ML 파이프라인                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
  │   데이터    │────▶│   라벨링    │────▶│   학습      │────▶│   배포      │
  │   수집      │     │ (Label     │     │ (Training)  │     │ (Inference) │
  │             │     │  Studio)   │     │             │     │             │
  └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                            │                   │
                            │◀──────────────────│
                            │  (Active Learning)
```

---

## 2. 전체 아키텍처

### 2.1 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      Label Studio 시스템 구성                                │
└─────────────────────────────────────────────────────────────────────────────┘

                         ┌─────────────────────────────────┐
                         │         Client (Browser)        │
                         │  ┌───────────────────────────┐  │
                         │  │    React Frontend         │  │
                         │  │    (SPA)                  │  │
                         │  └───────────────────────────┘  │
                         └─────────────┬───────────────────┘
                                       │
                                       │ HTTP/HTTPS
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Label Studio Server                                │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         Django Application                            │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │   Views     │  │   Models    │  │   APIs      │  │  Middleware │  │  │
│  │  │             │  │             │  │   (DRF)     │  │             │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                       │                                     │
│  ┌────────────────────────────────────┼────────────────────────────────┐   │
│  │                                    ▼                                │   │
│  │  ┌─────────────┐  ┌─────────────────────────────┐  ┌─────────────┐ │   │
│  │  │  Database   │  │      File Storage           │  │   Redis     │ │   │
│  │  │ (PostgreSQL)│  │  (Local/S3/GCS/Azure)       │  │  (Optional) │ │   │
│  │  └─────────────┘  └─────────────────────────────┘  └─────────────┘ │   │
│  │                           Storage Layer                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       │ (Optional)
                                       ▼
                         ┌─────────────────────────────┐
                         │      ML Backend             │
                         │  (Prediction Service)       │
                         └─────────────────────────────┘
```

### 2.2 주요 컴포넌트

| 컴포넌트 | 기술 | 역할 |
|---------|------|------|
| **Frontend** | React, MobX | 라벨링 UI, 프로젝트 관리 |
| **Backend** | Django, DRF | API, 비즈니스 로직 |
| **Database** | PostgreSQL/SQLite | 데이터 저장 |
| **Storage** | Local/S3/GCS | 파일 저장 |
| **ML Backend** | Flask/FastAPI | 예측 서비스 (옵션) |

### 2.3 요청 처리 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         요청 처리 흐름                                       │
└─────────────────────────────────────────────────────────────────────────────┘

  Browser Request
        │
        ▼
  ┌─────────────┐
  │   Nginx     │  (Reverse Proxy - Production)
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │  Gunicorn   │  (WSGI Server)
  └──────┬──────┘
         │
         ▼
  ┌─────────────────────────────────────────────────────────────┐
  │                    Django Application                       │
  │                                                             │
  │  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐   │
  │  │ Middleware  │────▶│    URL      │────▶│    View     │   │
  │  │   Stack     │     │  Resolver   │     │  (or API)   │   │
  │  └─────────────┘     └─────────────┘     └──────┬──────┘   │
  │                                                  │         │
  │        ┌─────────────────────────────────────────┤         │
  │        │                                         │         │
  │        ▼                                         ▼         │
  │  ┌─────────────┐                          ┌─────────────┐  │
  │  │   Models    │                          │  Serializer │  │
  │  │   (ORM)     │                          │   (DRF)     │  │
  │  └──────┬──────┘                          └─────────────┘  │
  │         │                                                  │
  └─────────┼──────────────────────────────────────────────────┘
            │
            ▼
  ┌─────────────┐
  │  Database   │
  │ (PostgreSQL)│
  └─────────────┘
```

---

## 3. 백엔드 구조

### 3.1 Django 앱 구조

Label Studio는 여러 Django 앱으로 구성되어 있습니다:

```
label_studio/
├── core/                    # 핵심 설정 및 유틸리티
│   ├── settings/           # Django 설정
│   │   ├── base.py        # 기본 설정
│   │   ├── label_studio.py # Label Studio 설정 (커스텀 오버라이드 포인트)
│   │   └── ...
│   ├── urls.py             # URL 라우팅
│   └── ...
│
├── users/                   # 사용자 관리
│   ├── models.py           # User 모델
│   ├── views.py            # 로그인, 회원가입
│   ├── api.py              # User API
│   └── ...
│
├── organizations/           # 조직(팀) 관리
│   ├── models.py           # Organization 모델
│   ├── views.py
│   └── ...
│
├── projects/                # 프로젝트 관리
│   ├── models.py           # Project 모델
│   ├── views.py            # 프로젝트 CRUD
│   ├── api.py              # Project API
│   └── ...
│
├── tasks/                   # 태스크(라벨링 대상)
│   ├── models.py           # Task 모델
│   ├── views.py
│   ├── api.py              # Task API
│   └── ...
│
├── annotations/             # 어노테이션(라벨링 결과)
│   ├── models.py           # Annotation 모델
│   ├── views.py
│   ├── api.py              # Annotation API
│   └── ...
│
├── ml/                      # ML 백엔드 연동
│   ├── models.py           # MLBackend 모델
│   ├── api.py
│   └── ...
│
├── data_manager/            # 데이터 매니저 UI
│   └── ...
│
├── webhooks/                # 웹훅
│   ├── models.py           # Webhook 모델
│   └── utils.py            # 웹훅 발송 (커스터마이징 포인트)
│
└── io_storages/             # 외부 스토리지 연동
    ├── s3/
    ├── gcs/
    ├── azure/
    └── ...
```

### 3.2 주요 Django 앱 설명

| 앱 | 역할 | 주요 모델 |
|----|------|----------|
| `users` | 사용자 인증/관리 | User |
| `organizations` | 팀/조직 관리 | Organization, OrganizationMember |
| `projects` | 프로젝트 관리 | Project |
| `tasks` | 라벨링 대상 관리 | Task |
| `annotations` | 라벨링 결과 관리 | Annotation |
| `ml` | ML 백엔드 연동 | MLBackend, Prediction |
| `webhooks` | 이벤트 알림 | Webhook |

### 3.3 미들웨어 스택

```python
# Django Middleware 처리 순서

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',           # 1. 보안
    'django.contrib.sessions.middleware.SessionMiddleware',    # 2. 세션
    'django.middleware.common.CommonMiddleware',               # 3. 공통
    'django.middleware.csrf.CsrfViewMiddleware',              # 4. CSRF
    'django.contrib.auth.middleware.AuthenticationMiddleware', # 5. 인증
    'django.contrib.messages.middleware.MessageMiddleware',    # 6. 메시지
    # ... 추가 미들웨어
    'label_studio_sso.middleware.JWTAutoLoginMiddleware',     # SSO 미들웨어 (커스텀)
]
```

**미들웨어 처리 흐름**:
```
Request → Security → Session → Common → CSRF → Auth → SSO → View
                                                              │
Response ← Security ← Session ← Common ← CSRF ← Auth ← SSO ←─┘
```

### 3.4 인증 백엔드

```python
# 인증 백엔드 우선순위

AUTHENTICATION_BACKENDS = [
    'label_studio_sso.backends.JWTAuthenticationBackend',  # 1. JWT (SSO)
    'rules.permissions.ObjectPermissionBackend',           # 2. 객체 권한
    'django.contrib.auth.backends.ModelBackend',           # 3. 기본 인증
]
```

**인증 처리 흐름**:
```
인증 요청
    │
    ▼
┌─────────────────┐
│ JWT Backend     │ ── JWT 토큰 있음? ──▶ JWT 검증 → 사용자 반환
└────────┬────────┘
         │ (토큰 없음)
         ▼
┌─────────────────┐
│ Object Backend  │ ── 객체 권한 확인
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Model Backend   │ ── username/password 확인
└─────────────────┘
```

---

## 4. 프론트엔드 구조

### 4.1 React 앱 구조

```
label_studio/frontend/
├── src/
│   ├── app/                  # 앱 진입점
│   ├── components/           # 공통 컴포넌트
│   ├── pages/               # 페이지 컴포넌트
│   │   ├── Projects/        # 프로젝트 목록
│   │   ├── DataManager/     # 데이터 매니저
│   │   ├── Settings/        # 설정
│   │   └── ...
│   ├── stores/              # MobX 스토어
│   ├── providers/           # Context Providers
│   └── utils/               # 유틸리티
│
├── dist/                    # 빌드 결과물
└── package.json
```

### 4.2 주요 페이지

| 페이지 | 경로 | 설명 |
|--------|------|------|
| Projects | `/projects` | 프로젝트 목록 |
| Data Manager | `/projects/{id}/data` | 태스크 관리 |
| Labeling | `/projects/{id}/data?labeling=1` | 라벨링 UI |
| Settings | `/projects/{id}/settings` | 프로젝트 설정 |

### 4.3 상태 관리 (MobX)

```javascript
// MobX Store 구조 예시

class ProjectStore {
    @observable projects = [];
    @observable currentProject = null;

    @action
    async loadProjects() {
        const response = await api.get('/api/projects/');
        this.projects = response.data;
    }

    @action
    setCurrentProject(project) {
        this.currentProject = project;
    }
}
```

### 4.4 Label Studio Editor

Label Studio의 핵심인 라벨링 에디터는 별도 패키지로 관리됩니다:

```
label-studio-frontend (별도 저장소)
├── src/
│   ├── components/
│   │   ├── ImageView/       # 이미지 라벨링
│   │   ├── TextView/        # 텍스트 라벨링
│   │   ├── AudioView/       # 오디오 라벨링
│   │   └── ...
│   ├── regions/             # 라벨링 영역 (BBox, Polygon 등)
│   └── tags/                # 라벨링 태그 정의
└── ...
```

---

## 5. 데이터 모델

### 5.1 핵심 모델 관계

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         데이터 모델 관계도                                    │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────┐
  │    User     │
  │             │
  │  - email    │
  │  - password │
  │  - is_super │
  └──────┬──────┘
         │ 1
         │
         │ N (created_by)
         ▼
  ┌─────────────┐         1         ┌─────────────────┐
  │Organization │◀─────────────────▶│OrganizationMember│
  │             │                   │                 │
  │  - title    │                   │ - user          │
  └──────┬──────┘                   │ - organization  │
         │ 1                        └─────────────────┘
         │
         │ N
         ▼
  ┌─────────────┐
  │   Project   │
  │             │
  │  - title    │
  │  - config   │◀─── label_config (XML)
  │  - org      │
  └──────┬──────┘
         │ 1
         │
         │ N
         ▼
  ┌─────────────┐         N         ┌─────────────┐
  │    Task     │◀─────────────────▶│ Prediction  │
  │             │                   │             │
  │  - data     │◀─── JSON          │  - result   │
  │  - project  │                   │  - model_v  │
  └──────┬──────┘                   │  - task     │
         │ 1                        └─────────────┘
         │
         │ N
         ▼
  ┌─────────────┐
  │ Annotation  │
  │             │
  │  - result   │◀─── JSON (라벨링 결과)
  │  - task     │
  │  - completed│
  │    _by      │◀─── User (작성자)
  └─────────────┘
```

### 5.2 주요 모델 필드

#### User 모델
```python
class User(AbstractUser):
    email = models.EmailField(unique=True)
    phone = models.CharField(max_length=256, blank=True)
    avatar = models.ImageField(upload_to='avatars/', blank=True)

    # 권한
    is_superuser = models.BooleanField(default=False)

    # API 토큰
    # Token 모델과 1:1 관계 (DRF Token)
```

#### Project 모델
```python
class Project(models.Model):
    title = models.CharField(max_length=500)
    description = models.TextField(blank=True)

    # 라벨링 설정 (XML)
    label_config = models.TextField(default='<View></View>')

    # 소속
    organization = models.ForeignKey(Organization, on_delete=CASCADE)
    created_by = models.ForeignKey(User, on_delete=SET_NULL)

    # 설정
    model_version = models.TextField(blank=True)  # ML 모델 버전
    sampling = models.CharField(max_length=100)   # 샘플링 방식
```

#### Task 모델
```python
class Task(models.Model):
    # 데이터
    data = models.JSONField()  # {"image": "http://...", "text": "..."}
    meta = models.JSONField(default=dict)

    # 관계
    project = models.ForeignKey(Project, on_delete=CASCADE)

    # 상태
    is_labeled = models.BooleanField(default=False)
    overlap = models.IntegerField(default=1)  # 중복 라벨링 횟수

    # 인덱싱
    inner_id = models.BigIntegerField()  # 프로젝트 내 순번
```

#### Annotation 모델
```python
class Annotation(models.Model):
    # 라벨링 결과
    result = models.JSONField(default=list)  # 라벨링 데이터

    # 관계
    task = models.ForeignKey(Task, on_delete=CASCADE)
    completed_by = models.ForeignKey(User, on_delete=SET_NULL)

    # 메타데이터
    lead_time = models.FloatField(default=0)  # 소요 시간(초)
    was_cancelled = models.BooleanField(default=False)

    # 검토
    ground_truth = models.BooleanField(default=False)

    # 타임스탬프
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

### 5.3 라벨링 결과 구조 (result 필드)

```json
// Annotation.result 예시 (이미지 바운딩 박스)
[
    {
        "id": "result_1",
        "type": "rectanglelabels",
        "from_name": "label",
        "to_name": "image",
        "original_width": 1920,
        "original_height": 1080,
        "value": {
            "x": 10.5,
            "y": 20.3,
            "width": 30.0,
            "height": 40.0,
            "rectanglelabels": ["Car"]
        }
    }
]
```

```json
// Annotation.result 예시 (텍스트 NER)
[
    {
        "id": "result_1",
        "type": "labels",
        "from_name": "label",
        "to_name": "text",
        "value": {
            "start": 0,
            "end": 5,
            "text": "Apple",
            "labels": ["Organization"]
        }
    }
]
```

---

## 6. API 구조

### 6.1 API 엔드포인트 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         API 엔드포인트 구조                                   │
└─────────────────────────────────────────────────────────────────────────────┘

/api/
├── /users/                      # 사용자 관리
│   ├── GET    /                 # 사용자 목록
│   ├── POST   /                 # 사용자 생성
│   └── GET    /{id}/            # 사용자 상세
│
├── /projects/                   # 프로젝트 관리
│   ├── GET    /                 # 프로젝트 목록
│   ├── POST   /                 # 프로젝트 생성
│   ├── GET    /{id}/            # 프로젝트 상세
│   ├── PATCH  /{id}/            # 프로젝트 수정
│   ├── DELETE /{id}/            # 프로젝트 삭제
│   │
│   └── /{id}/
│       ├── /tasks/              # 태스크 관리
│       ├── /annotations/        # 어노테이션 관리
│       ├── /export              # 데이터 내보내기
│       └── /import              # 데이터 가져오기
│
├── /tasks/                      # 태스크 관리
│   ├── GET    /{id}/            # 태스크 상세
│   ├── PATCH  /{id}/            # 태스크 수정
│   └── DELETE /{id}/            # 태스크 삭제
│
├── /annotations/                # 어노테이션 관리
│   ├── GET    /{id}/            # 어노테이션 상세
│   ├── PATCH  /{id}/            # 어노테이션 수정
│   └── DELETE /{id}/            # 어노테이션 삭제
│
├── /ml/                         # ML 백엔드
│   └── ...
│
└── /webhooks/                   # 웹훅
    └── ...
```

### 6.2 인증 방식

**Token 인증** (API 호출):
```bash
curl -H "Authorization: Token YOUR_API_TOKEN" \
     http://localhost:8080/api/projects/
```

**Session 인증** (브라우저):
```
# Django Session 기반
# 쿠키: sessionid, csrftoken
```

**JWT 인증** (SSO):
```
# 쿠키: ls_auth_token
# 미들웨어가 자동으로 세션으로 변환
```

### 6.3 주요 API 예시

#### 프로젝트 생성
```bash
POST /api/projects/
Content-Type: application/json

{
    "title": "Image Classification",
    "label_config": "<View><Image name=\"image\" value=\"$image\"/><Choices name=\"choice\" toName=\"image\"><Choice value=\"Cat\"/><Choice value=\"Dog\"/></Choices></View>"
}
```

#### 태스크 추가
```bash
POST /api/projects/{project_id}/import
Content-Type: application/json

[
    {"image": "https://example.com/image1.jpg"},
    {"image": "https://example.com/image2.jpg"}
]
```

#### 어노테이션 생성
```bash
POST /api/tasks/{task_id}/annotations/
Content-Type: application/json

{
    "result": [
        {
            "type": "choices",
            "from_name": "choice",
            "to_name": "image",
            "value": {"choices": ["Cat"]}
        }
    ]
}
```

#### 데이터 내보내기
```bash
GET /api/projects/{project_id}/export?exportType=JSON
```

---

## 7. 확장 포인트

### 7.1 커스터마이징 가능 영역

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         커스터마이징 포인트                                   │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │                           Frontend Layer                                │
  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
  │  │ Template 오버라이드│  │ CSS 커스터마이징  │  │ JS 주입          │        │
  │  │ (base.html)     │  │                 │  │ (hideHeader 등) │        │
  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
  └─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                            API Layer                                    │
  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
  │  │ URL 오버라이드    │  │ View 오버라이드   │  │ 새 API 추가      │        │
  │  │ (urls.py)       │  │ (ViewSet 상속)  │  │ (custom-api/)  │        │
  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
  └─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                         Middleware Layer                                │
  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
  │  │ 인증 미들웨어     │  │ 보안 미들웨어     │  │ 커스텀 미들웨어   │        │
  │  │ (SSO)          │  │ (CSP, CORS)    │  │                 │        │
  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
  └─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                          Model Layer                                    │
  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
  │  │ Signal 훅       │  │ Permission 확장  │  │ Manager 커스텀   │        │
  │  │ (post_save 등)  │  │                 │  │                 │        │
  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
  └─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                         Settings Layer                                  │
  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
  │  │ Django 설정      │  │ 환경 변수        │  │ Feature Flags   │        │
  │  │ (label_studio.py)│ │ (.env)         │  │                 │        │
  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
  └─────────────────────────────────────────────────────────────────────────┘
```

### 7.2 확장 포인트별 상세

| 레이어 | 확장 방법 | 예시 |
|--------|----------|------|
| **Settings** | `label_studio.py` 오버라이드 | SSO 설정, 미들웨어 추가 |
| **Middleware** | MIDDLEWARE 리스트에 추가 | JWT 인증, CSP 헤더 |
| **URL** | `urls.py`에서 우선 등록 | 커스텀 API 엔드포인트 |
| **View** | ViewSet 상속 및 오버라이드 | 어노테이션 권한 변경 |
| **Permission** | Permission 클래스 구현 | 소유권 기반 접근 제어 |
| **Signal** | post_save, pre_delete 훅 | 자동 조직 할당 |
| **Template** | templates/ 오버라이드 | hideHeader, UI 변경 |

### 7.3 Django Signal 활용

```python
# signals.py 예시

from django.db.models.signals import post_save
from django.dispatch import receiver
from users.models import User
from organizations.models import Organization, OrganizationMember

@receiver(post_save, sender=User)
def auto_assign_organization(sender, instance, created, **kwargs):
    """새 사용자 생성 시 자동으로 조직에 추가"""
    if created:
        default_org = Organization.objects.first()
        if default_org:
            OrganizationMember.objects.create(
                user=instance,
                organization=default_org
            )
```

### 7.4 Webhook 이벤트

| 이벤트 | 발생 시점 | 페이로드 |
|--------|----------|----------|
| `ANNOTATION_CREATED` | 어노테이션 생성 | annotation 객체 |
| `ANNOTATION_UPDATED` | 어노테이션 수정 | annotation 객체 |
| `ANNOTATION_DELETED` | 어노테이션 삭제 | annotation ID |
| `TASK_CREATED` | 태스크 생성 | task 객체 |
| `PROJECT_CREATED` | 프로젝트 생성 | project 객체 |

**Webhook 페이로드 예시**:
```json
{
    "action": "ANNOTATION_CREATED",
    "annotation": {
        "id": 123,
        "result": [...],
        "completed_by": 1,
        "completed_by_info": {  // 커스텀 확장
            "id": 1,
            "email": "user@example.com",
            "is_superuser": false
        }
    },
    "project": {
        "id": 1,
        "title": "My Project"
    },
    "task": {
        "id": 456,
        "data": {...}
    }
}
```

---

## 다음 단계

Label Studio의 기본 구조를 이해했다면:

1. [커스터마이징 방법론](./02-customization-methodology.md)에서 실제 커스터마이징 방법을 학습하세요.
2. [SSO 구현 방법](./03-sso-implementation.md)에서 인증 통합 방법을 확인하세요.
